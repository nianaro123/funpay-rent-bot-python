# funpay-rent-bot-python
bot for rent steam accounts on funpay

## Auto raise lots

Можно включить автоподнятие всех лотов из таблицы `goods`:

- `AUTO_RAISE_ENABLED=true`
- `AUTO_RAISE_INTERVAL_SEC=7200` (интервал в секундах, по умолчанию 120 минут)
